var scoreToTally = function(score){
  let quotient = Math.floor(score/5);
  let remainder = score % 5;
  let r = [quotient, remainder];
  let letter = ['','a','b','c','d','e'];
  
  return ('e' + ' <br>').repeat(quotient) + letter[remainder];
}
